uint64 timestamp                         # time since system start (microseconds)

bool active                              # communication-loss handling is active
bool midcourse_recovery_active           # preserve and restore the interrupted midcourse session
bool terminal_guidance_inhibited         # terminal guidance is active, so link-loss handling is inhibited
