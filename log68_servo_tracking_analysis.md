# log68 导引头伺服状态与跟踪状态分析

源日志：`/home/btfw/下载/log_68_2026-8-31-19-23-18.ulg`

时间均为相对 ULog 开始记录的秒数。

## 结论

末制导拨杆和飞控到导引头的指令链是通的：每次拨杆后，`dyt_guidance` 都进入 `SEARCH_WAIT_LOCK`，导引头上报的伺服模式 `status1` 也从 `0x03` 切换到了 `0x06`。

未锁定的直接判据是：导引头没有在跟踪窗口内把原始跟踪状态 `status2` 从 `0x00` 变成参数 `DYT_TRK_VAL` 要求的 `0x01`。因此飞控收到的是“已经进入跟踪模式，但目标尚未锁定”。

按当前导引头新版协议，持续识别与 `0x06` 锁定触发已经分开；`0x06` 是一次性的锁定请求，不要求飞控把整个 2.5 秒都保持成 `0x06`。当前时序为：

1. 末制导触发后，导引头进入 `status1=0x06` 跟踪模式。
2. 驱动只在 `1.2 s` 的 `TRACKING_MODE_GUARD` 内阻止中制导指向命令覆盖跟踪模式。
3. 如果导引头没有在这 `1.2 s` 内回报 `status2=0x01`，后续中制导指向命令可以把伺服模式重新改成 `status1=0x03`。
4. `DYTG_WAITMS=2500 ms` 只是飞控状态机的最终超时，不代表导引头需要持续 2.5 秒的 `0x06` 模式。

所以当前失败过程实际是：**导引头已经处于持续识别状态；末制导拨杆发送 `0x06` 请求锁定当前识别目标，导引头也用 `status1=0x06` 证明收到了请求，但没有在 1.2 秒内完成锁定并回报 `status2=0x01`，最后飞控状态机以超时结束。** 后续恢复 `0x03` 会阻止更晚的锁定完成，但按“`0x06` 应快速完成一次性锁定”的协议前提，它不能解释为什么多次请求在最初 1.2 秒内都没有成功。

日志中也存在成功锁定，证明 `DYT_TRK_VAL=1`、状态帧解析和末制导入口本身是正确的。不能仅凭这份日志进一步判断导引头最初 1.2 秒为什么没有完成锁定，因为当前状态协议在锁定前没有把识别候选框写入 `dyt_target`，失败期间也没有目标框或候选目标反馈可供日志判断。

## 当前关键参数

| 参数 | 日志值 | 含义 |
|---|---:|---|
| `DYTG_ACT_AUX` | -1 | AUX 触发关闭 |
| `DYTG_ACT_BTN` | 2 | 使用 `MANUAL_CONTROL.buttons` 的 bit 2 触发末制导 |
| `CRDZ_ACT_BTN` | 0 | bit 0 为中制导触发 |
| `DYTG_LOCK_N` | 2 | 连续 2 个可用锁定周期后进入末制导跟踪 |
| `DYTG_WAITMS` | 2500 ms | 等待锁定超时 |
| `DYT_TRK_VAL` | 1 | 原始跟踪状态必须为 `0x01` |
| `DYTG_MAXAGE` | 0.5 s | 目标数据最大允许年龄 |
| `DYTG_RELOCKN` | 40 | 丢锁确认周期；超出当前参数文档标注的最大值 20，但不影响本次初次锁定失败 |

## 状态字段解码

| 日志字段 | 原始协议字段 | 本日志中的主要值 | 解释 |
|---|---|---|---|
| `dyt_target.status1` | 伺服状态帧 byte 6 | `0x03`、`0x06` | `0x03` 随动角度模式；`0x06` 跟踪模式 |
| `dyt_target.status2` | 伺服状态帧 byte 31 | `0x00`、`0x01` | 原始跟踪状态；当前参数要求 `0x01` 才算锁定 |
| `dyt_target.status3` | 伺服状态帧 byte 5 | `0x01` | 可见光视频源状态 |
| `tracking_state` | PX4 解码结果 | 0、1、2 | 0 搜索；1 锁定；2 通信超时 |
| `target_valid` | PX4 解码结果 | 0、1 | 同时满足锁定回报和有效视线角才为 1 |
| `bbox_width/height` | 伺服状态帧 byte 40～43 | `NaN` 或有效像素 | 只有锁定时才输出有效目标框 |

飞控认定锁定的代码条件为：

```text
status1 == 0x06
并且
status2 == DYT_TRK_VAL（本日志为 0x01）
```

## 每次末制导拨杆结果

`按钮 bit 2=1` 表示末制导拨杆有效。

| 次数 | 拨杆有效区间 | `0x06` 开始 | 首次 `status2=0x01` | 结果 |
|---:|---:|---:|---:|---|
| 1 | 23.086～26.237 s | 22.966 s | 无 | 失败；约 2.5 s 后 `LOST_REASON_TIMEOUT` |
| 2 | 27.686～30.884 s | 27.766 s | 无 | 失败；超时 |
| 3 | 32.077～34.077 s | 32.066 s | 32.276 s | 成功锁定约 0.7 s，随后丢锁 |
| 4 | 37.077～40.684 s | 37.166 s | 无 | 失败；超时 |
| 5 | 41.884～44.077 s | 41.876 s | 42.276 s | 成功锁定约 0.3 s，随后目标数据失效 |
| 6 | 151.686～155.637 s | 151.661 s | 无 | 失败；超时 |
| 7 | 157.286～160.485 s | 157.271 s | 无 | 失败；超时 |
| 8 | 164.686～168.237 s | 164.771 s | 无 | 失败；超时 |
| 9 | 170.077～173.878 s | 170.071 s | 无 | 失败；超时 |
| 10 | 175.237～178.286 s | 175.271 s | 无 | 失败；超时 |
| 11 | 180.086～186.686 s | 180.071 s | 无 | 失败；超时 |
| 12 | 188.485～193.077 s | 188.371 s | 无 | 失败；超时 |
| 13 | 193.877～195.485 s | 193.871 s | 无 | 拨杆在超时前关闭 |
| 14 | 196.685～198.078 s | 196.571 s | 无 | 拨杆在超时前关闭 |
| 15 | 200.886 s～日志结束 | 200.771 s | 200.981 s | 200.981 s 短暂锁定一帧；201.181～233.761 s 持续锁定 |

## 导引头上报的关键状态区间

### `status1=0x06` 跟踪模式区间

| 开始 | 结束 | 帧数 | 是否收到锁定状态 |
|---:|---:|---:|---|
| 22.966 | 24.066 | 12 | 否 |
| 27.766 | 28.876 | 12 | 否 |
| 32.066 | 33.176 | 12 | 是 |
| 37.166 | 38.276 | 12 | 否 |
| 41.876 | 42.576 | 8 | 是 |
| 151.661 | 152.771 | 12 | 否 |
| 157.271 | 158.371 | 12 | 否 |
| 164.771 | 165.871 | 12 | 否 |
| 170.071 | 171.271 | 13 | 否 |
| 175.271 | 176.371 | 12 | 否 |
| 180.071 | 181.171 | 12 | 否 |
| 188.371 | 189.561 | 13 | 否 |
| 193.871 | 194.971 | 12 | 否 |
| 196.571 | 197.671 | 12 | 否 |
| 200.771 | 233.761 | 331 | 是 |

除最后一次持续锁定外，失败尝试的 `0x06` 区间几乎都只有约 `1.1～1.2 s`，随后恢复为 `status1=0x03`，与代码中的 `TRACKING_MODE_GUARD=1200 ms` 完全对应。按照新版协议，这段时间是给一次性 `0x06` 锁定请求留下的保护窗口，并不要求扩展到整个 `DYTG_WAITMS`。

### `status2=0x01` 原始锁定回报区间

| 开始 | 结束 | 帧数 | PX4 解码结果 |
|---:|---:|---:|---|
| 32.276 | 32.976 | 8 | 锁定 |
| 42.276 | 42.866 | 7 | 42.676 s 后因 `status1` 已变成 `0x03`，不再满足锁定组合条件 |
| 200.981 | 200.981 | 1 | 短暂锁定一帧 |
| 201.181 | 234.171 | 331 | 201.181～233.761 s 同时满足 `status1=0x06`，为持续有效锁定 |

## 失败时的共同特征

失败尝试均具有以下特征：

- 末制导按钮 bit 2 已经变为 1。
- `dyt_guidance.state` 已进入 `SEARCH_WAIT_LOCK`。
- 导引头确实上报 `status1=0x06`，说明跟踪指令已到达导引头。
- `status2` 始终为 `0x00`，未上报协议定义的锁定值 `0x01`。
- `tracking_state=SEARCH`、`target_valid=0`。
- `bbox_width_px` 和 `bbox_height_px` 均为 `NaN`，导引头没有回传有效识别框。
- `auto_hint` 在整个日志中始终为 0。这不代表导引头没有在识别，只表示当前上报和驱动没有把锁定前的识别候选状态暴露给 PX4。
- 约 1.2 秒后 `status1` 返回 `0x03`；在此之前导引头已经完整收到一次 `0x06` 锁定请求，但没有返回锁定成功。
- 约 2.5 秒后状态机进入 `LOST_HOLD`，原因是 `LOST_REASON_TIMEOUT`。

## 可以排除的原因

- 不是末制导拨杆未被飞控识别：按钮 bit 2 和状态机变化完全对应。
- 不是跟踪命令没有发到导引头：每次都能看到 `status1=0x06`。
- 不是 `DYT_TRK_VAL` 配错：同一日志中导引头成功锁定时确实回报 `status2=0x01`。
- 不是串口持续丢帧：3090 帧中 `parse_error_count` 仅在最后 9 帧为 1，失败锁定区间没有持续解析错误。
- 不是数据陈旧：等待锁定期间 `target_fresh=1`。

## 最终判断

本次“拨末制导但没有锁定”的首要表现是：导引头虽然一直在识别，而且已经收到 `0x06` 锁定请求，却没有在保护窗口内返回锁定状态和目标框。同一日志中相同命令又能成功锁定，说明飞控指令链和解析规则基本正确；更可能是导引头内部在部分时刻没有选中可锁目标、锁定条件未满足，或新版 `0x06` 请求的完整帧语义仍需与导引头端逐字节核对。

不建议在没有协议依据时直接把保护时间扩展到 2.5 秒，也不建议持续重复发送 `0x06`，因为重复命令可能重新启动导引头内部锁定流程。下一步应先确认导引头要求的 `0x06` 完整帧、是否需要目标编号/坐标，以及锁定失败时应上报的状态字节。本文只分析日志，未修改代码。
