# 导引头融合测距、测速与网捕时序说明

本文档描述当前 `dyt_guidance` 代码中的实际逻辑。文中“有效”指软件判据通过，不等同于传感器真实误差已经满足任务要求。

## 1. 关键常量与参数

| 名称 | 当前值 | 含义 |
|---|---:|---|
| 图像距离有效范围 | 1～50 m | 长边拟合距离的有效范围 |
| 图像接近速度有效范围 | 0～75 m/s | 正值表示正在接近目标 |
| 图像测速窗口 | 250 ms | 线性拟合最多使用最近250 ms样本 |
| 图像测速最少样本数 | 3 | 少于3个样本时测速无效 |
| 图像测速最小时间跨度 | 150 ms | 样本覆盖时间不足时测速无效 |
| 图像测速最大样本间隔 | 250 ms | 超过后清空测速历史 |
| 图像测速最大年龄 | 500 ms | 超时后测速失效 |
| 激光最大年龄 | 50 ms | 超过后激光不参与融合 |
| 距离修正系数范围 | 0.5～1.5 | 激光距离/图像距离 |
| 距离系数建立样本数 | 连续2个 | 必须是两个新的有效激光样本 |
| 速度修正系数范围 | 0.5～1.5 | 激光接近速度/图像接近速度 |
| 速度系数建立样本数 | 连续2个 | 必须是两个新的有效激光样本 |
| 距离/速度更新系数 | 0.15 | 一阶平滑权重 |
| 修正图像与激光一致性门限 | 1.5 m | 超过后不采用激光距离 |
| 修正图像速度与激光速度门限 | 5 m/s | 超过后不采用激光速度 |
| `DYTG_FIRE_D` | 默认2 m | 网捕基础距离；设备保存值优先 |
| 姿态提前时间 | 0.3 s | 姿态调整距离随速度增加 |
| PWM提前时间 | 0.05 s | PWM距离随速度增加 |
| `DYTG_SZ_MS` | 默认500 ms | 单次自动姿态调整最大时间 |
| PWM后姿态保持 | 50 ms | PWM发出后继续保持网捕姿态 |
| 未发射超时悬停 | 2 s | 减速完成后保持2 s再自动进入中制导 |
| `DYTG_STOP_V` | 默认0.2 m/s | 低于该水平速度后进入定点保持 |

PX4通常保留设备中已经保存的参数。因此修改参数默认值后，应通过 `param show` 核对实际值。

## 2. 图像测距

### 2.1 距离计算

当前控制使用目标框长边拟合：

```text
long_ratio = max(bbox_width_px, bbox_height_px) / image_width_px

image_distance_long_m = 1.2075 / long_ratio - 0.6356
```

面积拟合距离仍会计算：

```text
area_ratio = bbox_width_px × bbox_height_px / (image_width_px × image_height_px)

image_distance_area_m = 1.5520 / sqrt(area_ratio) - 0.5923
```

面积拟合及其与长边拟合的差值只用于诊断，不参与融合有效判断，也不直接触发网捕。

### 2.2 图像距离有效条件

图像距离只有同时满足以下条件才有效：

1. 已收到目标，目标时间戳非零且不晚于当前时间。
2. 视频源是支持的可见光或红外源。
3. 变焦倍率有限，且与1倍的偏差不超过0.05，即约0.95～1.05倍。
4. 图像宽、高配置均大于0。
5. 目标框宽、高有限，且均不小于4 px。
6. 目标框没有超过图像尺寸。
7. 面积比例和长边比例有限，且处于 `(0, 1]`。
8. 长边拟合距离有限，且处于1～50 m。

视频源切换、目标框非法或图像距离越界时，会清除图像测速历史和距离融合状态。

## 3. 图像测速

图像测速使用最近250 ms内的图像长边距离做最小二乘直线拟合：

```text
distance_rate = 距离对时间的拟合斜率
image_closing_speed = -distance_rate
```

因此距离减小时接近速度为正。

图像测速有效必须同时满足：

1. 图像距离有效。
2. 最近窗口内至少有3个有效样本。
3. 最早和最新样本时间跨度至少150 ms。
4. 拟合分母有效，计算结果不是NaN或Inf。
5. 接近速度处于0～75 m/s。
6. 最新有效测速年龄不超过500 ms。

新图像样本相对上一个样本倒退、重复进入历史，或间隔超过250 ms时，测速历史会被清空。没有新图像帧时不会重复加入测速样本，但融合和激光预测仍会在模块周期内重新计算。

## 4. 激光数据与50 ms预测

### 4.1 激光测量有效条件

激光测量必须满足：

1. `DYTG_FUS_EN > 0`。
2. `sdm50_status.valid == true`。
3. 样本时间戳非零、不晚于当前时间。
4. 样本年龄不超过50 ms。
5. 原始激光距离有限且处于0.05～50 m。

超过50 ms后，激光立即不参与距离或速度融合。

### 4.2 年龄补偿预测

如果激光接近速度有限且处于0～75 m/s，则在50 ms有效期内计算：

```text
laser_age_s = (now - laser_timestamp_sample) / 1e6

predicted_laser_distance =
    measured_laser_distance - laser_closing_speed × laser_age_s
```

预测距离还必须处于0.05～50 m。若激光速度无效，则50 ms内使用原始激光距离，不进行外推。

预测值只用于：

- 融合距离输出；
- 与修正图像距离的1.5 m一致性判断；
- 姿态调整和PWM触发判据。

距离和速度乘法修正系数只由真实的新激光样本更新，预测值不会反馈到标定状态。

## 5. 融合测距

### 5.1 乘法系数初始化

每个新的有效激光样本计算原始系数：

```text
raw_scale = measured_laser_distance / image_distance
```

初始化有效条件：

1. 图像距离和图像测速均有效。
2. 激光测量有效且是新样本。
3. `raw_scale`有限。
4. `0.5 <= raw_scale <= 1.5`。
5. 连续两个新激光样本均满足上述条件。

建立后的初始系数为：

```text
distance_scale = (raw_scale_1 + raw_scale_2) / 2
```

初始化期间任意新的激光样本系数越界，会清空计数并重新开始。初始化阶段不再使用固定3 m距离差门限，也不要求两个初始化样本之间满足1.5 m距离差。

### 5.2 激光优先输出

系数建立后先计算：

```text
corrected_image_distance = image_distance × distance_scale
```

如果：

```text
abs(corrected_image_distance - predicted_laser_distance) <= 1.5 m
```

则：

```text
fused_distance = predicted_laser_distance
laser_fusion_used = true
```

若同时是新的真实激光样本，且原始系数仍位于0.5～1.5，则更新：

```text
distance_scale =
    distance_scale + 0.15 × (raw_scale - distance_scale)
```

### 5.3 激光不可用或不一致时的回退

出现以下任一情况时不采用激光：

- 激光年龄超过50 ms；
- 激光状态、距离或预测距离无效；
- 修正图像距离与预测激光距离相差超过1.5 m；
- 距离修正系数尚未建立。

回退规则：

```text
系数已建立：fused_distance = image_distance × distance_scale
系数未建立：fused_distance = image_distance
```

最终融合距离有效条件：

```text
fused_distance是有限数
并且 0.05 m <= fused_distance <= 50 m
```

## 6. 融合测速

测速使用与距离修正相同形式的乘法系数：

```text
raw_speed_scale = laser_closing_speed / image_closing_speed
```

激光速度只有在激光距离已经通过融合一致性判断并被采用时才可能被采用。激光速度还必须有限且处于0～75 m/s。
原始速度系数必须处于0.5～1.5；连续两个有效的新激光样本建立初始速度系数。

修正图像速度为：

```text
corrected_image_speed =
    image_closing_speed × speed_scale
```

如果：

```text
abs(corrected_image_speed - laser_closing_speed) <= 5 m/s
```

则融合速度采用激光速度：

```text
fused_closing_speed = laser_closing_speed
```

新的真实激光样本到来时，速度系数按以下方式更新：

```text
speed_scale = speed_scale + 0.15 × (raw_speed_scale - speed_scale)
```

如果激光速度没有被采用：

```text
系数已建立：fused_closing_speed = image_closing_speed × speed_scale
系数未建立：fused_closing_speed = image_closing_speed
```

最终融合速度有效条件：

```text
fused_closing_speed是有限数
并且 0 <= fused_closing_speed <= 75 m/s
```

## 7. 融合结果整体有效判据

自动网捕估计器返回有效需要：

```text
image_range_valid
&& image_closing_speed_valid
&& fused_range_valid
&& fused_closing_speed_valid
```

这意味着：即使激光距离有效，只要图像测速尚未建立，融合结果仍会被标为无效，自动网捕距离判据不会执行。

关闭 `DYTG_FUS_EN` 时，图像距离和图像速度直接作为融合结果，但仍必须先通过各自有效条件。

## 8. 自动网捕判据

定义：

```text
D0 = 有效的DYTG_FIRE_D，代码约束范围0.1～12 m，默认2 m
D  = fused_distance
V  = fused_closing_speed，正值表示接近
```

### 8.1 姿态调整开始

```text
attitude_trigger_distance = D0 + 0.3 × V

D < attitude_trigger_distance
```

使用严格小于号。只有目标可用、自动释放已启用、融合距离和速度均有效、尚未释放且未处于超时恢复流程时，才开始姿态调整。

### 8.2 姿态调整期间

姿态调整期间每个模块周期都会：

1. 重新计算50 ms激光预测和融合结果。
2. 根据最新目标视线重新计算姿态调整指令。
3. 重新判断PWM距离条件。

`DYTG_SZ_MS`默认500 ms，是单次自动姿态调整的硬上限。

### 8.3 PWM发送

```text
fire_distance = D0 + 0.05 × V

D < fire_distance
```

条件满足时发送一次 `VEHICLE_CMD_DO_GRIPPER`释放命令，并将本次网捕标记为已经触发。PWM判据同样使用严格小于号。

### 8.4 自动姿态调整结束

存在两种结束条件，哪个先到就采用哪个：

1. 姿态调整时间达到500 ms；
2. PWM发出后达到50 ms。

如果500 ms超时仍未发送PWM，系统不再继续本次网捕，而是进入减速悬停恢复流程；速度降到悬停阈值后保持2 s，然后自动请求中制导。该分支不会标记网捕成功，也不会改变飞行器类型。

## 9. PWM后的刹车与悬停

PWM发出后的前50 ms仍保持网捕姿态。50 ms结束后，在 `DYTG_HOLD_EN > 0`、Offboard仍有效且前置条件满足时，进入网捕后刹车：

```text
水平速度目标 = 0
垂直速度目标 = 0
高度 = 保持当前高度
水平加速度 = 当前水平速度反方向的制动加速度
```

制动加速度按停止距离估算并限幅：

```text
required_accel = horizontal_speed² / (2 × DYTG_STOP_D)
```

当水平速度满足：

```text
horizontal_speed <= DYTG_STOP_V
```

系统才会捕获当前位置和航向、停止目标跟踪、进入 `Idle`，并发布定点保持指令。默认停止速度为0.2 m/s。因此PWM后50 ms不是立即悬停，而是“开始刹车”；速度降到阈值后才进入悬停。

## 10. 完整时间顺序

```text
目标框有效
  ↓
图像距离进入1～50 m
  ↓
积累至少3个图像样本，时间跨度至少150 ms
  ↓
图像测速有效，融合结果才可能整体有效
  ↓
有新激光时：连续两个0.5～1.5系数样本建立乘法系数
  ↓
激光年龄≤50 ms：使用速度预测的激光距离
激光失效/不一致：回退到修正图像距离或原始图像距离
  ↓
D < D0 + 0.3V
  ↓
开始姿态调整，启动最长500 ms窗口
  ↓
每周期更新姿态、激光预测、融合距离和融合速度
  ↓
D < D0 + 0.05V
  ↓
发送一次网捕PWM释放命令
  ↓
继续保持网捕姿态50 ms
  ↓
进入水平刹车并保持高度
  ↓
水平速度≤DYTG_STOP_V
  ↓
停止跟踪，进入定点悬停
```

如果500 ms窗口内始终没有满足PWM条件：

```text
结束姿态调整
  ↓
水平刹车并保持高度
  ↓
水平速度≤DYTG_STOP_V后进入悬停
  ↓
悬停保持2 s
  ↓
自动进入中制导
```

## 11. 手动网捕与自动网捕的区别

手动触发通过配置的辅助通道或按钮上升沿进入。手动流程保留固定250 ms的PWM等待方式，并绕过自动融合距离的0.3 s/0.05 s触发逻辑。自动超时锁止不禁止手动释放。

## 12. 主要状态字段

`dyt_guidance_status`中可重点记录：

| 字段 | 含义 |
|---|---|
| `image_distance_long_m` | 原始图像长边拟合距离 |
| `image_closing_speed_m_s` | 图像拟合接近速度 |
| `laser_distance_m` | 当前参与融合的年龄补偿激光距离 |
| `laser_closing_speed_m_s` | 当前接受的激光接近速度 |
| `image_distance_scale` | 图像距离乘法修正系数 |
| `image_speed_scale` | 图像速度乘法修正系数 |
| `fused_distance_m` | 最终融合距离 |
| `fused_closing_speed_m_s` | 最终融合接近速度 |
| `image_trigger_distance_m` | 姿态调整触发距离 `D0 + 0.3V` |
| `laser_fusion_used` | 当前融合距离是否直接采用预测激光 |
| `fused_range_valid` | 最终距离是否有效 |
| `fused_closing_speed_valid` | 最终速度是否有效 |
| `net_trigger_sent` | 是否已经发送网捕释放命令 |
| `net_trigger_count` | 当前模块生命周期内的释放命令计数 |

## 13. 安全验证重点

该逻辑直接影响姿态指令、Offboard轨迹设定值和网捕执行器，属于高风险飞控功能。上机前至少应验证：

1. 激光20～40 ms正常采样和超过50 ms丢失时的切换。
2. 预测激光与修正图像距离跨越1.5 m门限时没有危险跳变。
3. 距离系数越界、NaN、Inf和图像框突变时能够安全回退。
4. 500 ms超时后正确减速，在悬停2 s后自动进入中制导，且不误标记为网捕成功。
5. PWM只发送一次，PWM后50 ms正确进入刹车流程。
6. Offboard退出、目标丢失、解锁或前置条件失效时不会继续输出危险指令。
7. 无桨台架记录完整的距离、速度、触发门限、姿态误差、释放命令和刹车状态。
